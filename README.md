# FortiCNAPP Container Images

Docker images for running FortiCNAPP scripts — self-contained with all dependencies baked in.

| Image | Description |
|-------|-------------|
| [lw-aws-inventory](lw-aws-inventory/) | AWS license vCPU sizing script (EC2, ECS Fargate, Lambda) |
